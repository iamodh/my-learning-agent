# 학습 문서

이 대화에서는 Node.js에서 이메일 발송 기능을 붙이기 위한 라이브러리로 `nodemailer`를 짧게 짚었다. 적용 맥락(어떤 흐름의 어느 시점에 호출되는지, 입력·출력 형태)이 대화에 드러나지 않아 결과 문서는 만들지 않았다.

## 문서 배치

```
.
├── README.md
└── nodemailer.md
```

- `nodemailer.md` — Node.js 이메일 발송 라이브러리 `nodemailer`의 개념과 사용 흐름(설치, transporter, sendMail) 요약.
