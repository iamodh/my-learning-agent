# 문서 배치

리액트에서 호출하는 Express API의 CORS 에러를 해결하기 위해 학습한 지식과, 그 결과로 정한 Express 측 설정을 정리한다.

```
.
├── knowledge/
│   ├── cors.md                       # CORS 정책의 원리와 프리플라이트
│   └── express-cors-middleware.md    # Express `cors` 미들웨어 사용법과 옵션
└── result/
    └── express-cors-setup.md         # 리액트 → Express API 호출용 CORS 설정
```
