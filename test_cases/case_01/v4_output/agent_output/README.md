# 학습 정리

리액트 앱에서 익스프레스 API를 호출하다 발생한 CORS 에러를 계기로, CORS가 무엇이고 왜 발생하는지, 익스프레스에서 `cors` 미들웨어로 어떻게 처리하는지, 그리고 프리플라이트(OPTIONS) 요청은 왜 먼저 가는지를 학습했다.

## 문서 배치

```
.
├── README.md
└── cors.md
```

- `cors.md` — CORS 개념, 에러가 나는 이유, 익스프레스 `cors` 미들웨어 사용법, 프리플라이트 동작
